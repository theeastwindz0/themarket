import React from 'react';
import ButtonContext from './ButtonContext'
const ButtonProvider = () => {
  return (
      <ButtonContext.ButtonProvider></ButtonContext.ButtonProvider>
  )
};

export default ButtonProvider;
